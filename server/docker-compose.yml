version: '3.8'
services:
  db:
    image: postgres:15
    container_name: feedback_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: feedback_flow
    ports:
      - '5432:5432'